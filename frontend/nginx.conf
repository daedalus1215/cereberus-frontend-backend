server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / {
    try_files $uri $uri/ /index.html;
  }
  # No /api proxy: Traefik splits that off to the backend before it reaches nginx.
}
